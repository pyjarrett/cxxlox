class Empty {}
print Empty;

var e = Empty();
print e;

class Vector {
    init(x, y) {
        this.x = x;
        this.y = y;
    }

    addVector(p) {
        this.x = this.x + p.x;
        this.y = this.y + p.y;
    }
    distance_to(p) {}
}
var p = Vector(10, 20);
print p;
print p.x;
print p.y;

var q = Vector(0, 0);
q.x = 30;
q.y = 40;

p.addVector(q);
print p.x;
print p.y;
